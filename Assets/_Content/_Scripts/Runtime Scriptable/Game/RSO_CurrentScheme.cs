using RuntimeScriptables;
using UnityEngine;

[CreateAssetMenu(fileName = "RSO_CurrentScheme", menuName = "Runtime Scriptable/Game/Current Scheme")]
public class RSO_CurrentScheme : RuntimeScriptableObject<InputScheme> { }